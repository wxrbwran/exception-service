server:
  port: 10086

spring:
  application:
    name: exception-cloud-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 103.105.200.216
        group: ${spring.profiles.active}
        namespace: ${spring.profiles.active}
        cluster-name: BJ
      config:
        server-addr: 103.105.200.216
        file-extension: yaml

